import 'package:flutter/material.dart';

import 'dimensions.dart';

class Paddings {
  Paddings._();

  /// all 4
  static const allXxxxs = EdgeInsets.all(D.xxxxs);

  /// all 8
  static const allXxxs = EdgeInsets.all(D.xxxs);

  /// all 12
  static const allXxs = EdgeInsets.all(D.xxs);

  /// all 16
  static const allXs = EdgeInsets.all(D.xs);

  /// all 20
  static const allSm = EdgeInsets.all(D.sm);

  /// all 24
  static const allMd = EdgeInsets.all(D.md);

  /// all 32
  static const allLg = EdgeInsets.all(D.lg);

  /// all 40
  static const allXl = EdgeInsets.all(D.xl);

  /// all 48
  static const allXxl = EdgeInsets.all(D.xxl);

  /// all 56
  static const allXxxl = EdgeInsets.all(D.xxxl);

  /// all 64
  static const allXxxxl = EdgeInsets.all(D.xxxxl);

  /// vertical 4
  static const verticalXxxxs = EdgeInsets.symmetric(vertical: D.xxxxs);

  /// vertical 8
  static const verticalXxxs = EdgeInsets.symmetric(vertical: D.xxxs);

  /// vertical 12
  static const verticalXxs = EdgeInsets.symmetric(vertical: D.xxs);

  /// vertical 16
  static const verticalXs = EdgeInsets.symmetric(vertical: D.xs);

  /// vertical 20
  static const verticalSm = EdgeInsets.symmetric(vertical: D.sm);

  /// vertical 24
  static const verticalMd = EdgeInsets.symmetric(vertical: D.md);

  /// vertical 32
  static const verticalLg = EdgeInsets.symmetric(vertical: D.lg);

  /// vertical 40
  static const verticalXl = EdgeInsets.symmetric(vertical: D.xl);

  /// vertical 48
  static const verticalXxl = EdgeInsets.symmetric(vertical: D.xxl);

  /// vertical 56
  static const verticalXxxl = EdgeInsets.symmetric(vertical: D.xxxl);

  /// vertical 64
  static const verticalXxxxl = EdgeInsets.symmetric(vertical: D.xxxxl);

  /// horizontal 4
  static const horizontalXxxxs = EdgeInsets.symmetric(horizontal: D.xxxxs);

  /// horizontal 8
  static const horizontalXxxs = EdgeInsets.symmetric(horizontal: D.xxxs);

  /// horizontal 12
  static const horizontalXxs = EdgeInsets.symmetric(horizontal: D.xxs);

  /// horizontal 16
  static const horizontalXs = EdgeInsets.symmetric(horizontal: D.xs);

  /// horizontal 20
  static const horizontalSm = EdgeInsets.symmetric(horizontal: D.sm);

  /// horizontal 24
  static const horizontalMd = EdgeInsets.symmetric(horizontal: D.md);

  /// horizontal 32
  static const horizontalLg = EdgeInsets.symmetric(horizontal: D.lg);

  /// horizontal 40
  static const horizontalXl = EdgeInsets.symmetric(horizontal: D.xl);

  /// horizontal 48
  static const horizontalXxl = EdgeInsets.symmetric(horizontal: D.xxl);

  /// horizontal 56
  static const horizontalXxxl = EdgeInsets.symmetric(horizontal: D.xxxl);

  /// horizontal 64
  static const horizontalXxxxl = EdgeInsets.symmetric(horizontal: D.xxxxl);

  /// horizontal 27
  static const commonSpacing = EdgeInsets.symmetric(horizontal: D.commonSpacing);
}
